# Re-kNN

> **"Vector search should not depend on dice."**

[Japanese README](./README.ja.md)

Re-kNN is an evaluation-oriented vector search and inference library that rethinks kNN within a modern vector search engine architecture, with a focus on deterministic geometry, dynamic updates, and explicit unknown detection.

## Quick Links

- **Evaluation License for Core DLL**: [`EULA-RekNN-Evaluation.md`](./EULA-RekNN-Evaluation.md)
- **Japanese Reference Translation of EULA**: [`EULA-RekNN-Evaluation.ja.md`](./EULA-RekNN-Evaluation.ja.md)
- **License for Sample Applications / Source Code**: [`LICENSE`](./LICENSE)

## Repository Structure

This repository intentionally separates the licensing and distribution model by component:

- **Core DLL**: distributed as a binary-only evaluation package under a proprietary evaluation license
- **Sample applications and related source code**: provided separately under Apache License 2.0
- **Documentation**: unless otherwise noted, follows the same licensing policy as the sample applications

Please review the applicable license before using each component.

## Manifesto

* **ReBuild**: Reconstructing the fundamental architecture of vector search engines from the ground up.
* **ReNewal**: Combining kNN with modern vector search engines to renew the algorithm for today's large-scale requirements.
* **ReCall**: Reducing unnecessary uncertainty in search and inference through deterministic search behavior and explicit unknown handling.
* **ReFine**: Continuous optimization of high-dimensional geometry through post-build refinement. Re-kNN can re-evaluate and correct elements placed in structurally inappropriate positions within the search tree after database construction.

## Core Philosophy

### Deterministic Geometry

Re-kNN performs search based on **Deterministic Geometry**, without relying on random numbers or random seeds.
Under identical input conditions, the system always yields identical results.

### Density-Based Partitioning

Spatial partitioning is performed based on the **natural density** of the data.

### No Data Truncation

**Clustering instead of quantization.**
Unlike traditional methods that truncate vector precision to save memory, Re-kNN employs high-fidelity clustering based on density. 
We preserve the original integrity of your data while organizing it with a structurally efficient approach.

### Sequential Order Acceptance

Re-kNN accepts that the tree structure may vary depending on the order of data insertion.
We embrace the "flow of time" as a structural reality.
Insertion order affects the resulting structure, but the system remains deterministic under a fixed insertion order.

### No Sort

Re-kNN does not rely on 1D sorting as the primary organizing principle for high-dimensional data.

### Honest Predict

Re-kNN provides supporting information for each inference, such as matched neighbors, distances, and unknown / warning states.

### Honest Unknown

Explicitly notifies when the target data is unknown. We prioritize "intellectual honesty" over forcing a guess.

### Dynamic Knowledge Maintenance

Knowledge is not static. Re-kNN allows for real-time modifications—addition, deletion, and optimization—ensuring the database evolves alongside the information it stores.

### Post-Build Self-Correction

After database construction, Re-kNN can re-evaluate elements that were placed in structurally inappropriate positions in the search tree and correct them through refinement.

### Supported Standard Dimensions
 
Re-kNN provides standardized support for the following vector dimensions to ensure immediate evaluation across key domains:
* **Text Domain**: 768 dimensions (Target: text embeddings generated by `bert-base-multilingual-cased`)
* **Pattern Domain**: 784 dimensions (Target: MNIST raw pixel vectors, without feature extraction)
* **Vision Domain**: 3072 dimensions (Target: CIFAR-10 raw pixel vectors, flattened from 32×32×3 RGB images)

For CIFAR-10 evaluation, Re-kNN uses the original image pixel values directly as 3072-dimensional vectors, rather than using embeddings or features extracted by another model.

These dimensions are provided as reference evaluation targets. For other dimensional requirements, Re-kNN's core geometry can be applied through custom implementation.

### Distribution

* **Format**: Provided as a DLL for .NET 8.
* **Verified Environment**: Windows 11 Professional
* **Customization**: For other dimensions, dataset formats, or specific architectural needs, please contact the author.

## Positioning

This library is provided as an **evaluation-oriented binary package** for assessing the capabilities of our vector search and inference technology.

The purpose of this release is to allow users to verify the fundamental behavior of the system — including data registration, search, and inference — and to evaluate its characteristics in their own environments and datasets.

At this stage, this library should be regarded as an **evaluation / preview release**, not as a fully packaged production-grade software component. Its primary role is to support technical evaluation, benchmarking, feasibility studies, and early-stage prototyping.

### Scope

This repository provides:

* the evaluation DLL of the library
* CLI-based sample applications
* source code for the sample applications
* benchmark and evaluation utilities for selected datasets and use cases

The core implementation of the library itself is **not open source** and is distributed in binary form only.

### Intended Use

This release is intended for:

* technical evaluation
* performance benchmarking
* feasibility studies
* experimental integration
* research and validation

This release is **not intended to guarantee production suitability, long-term API stability, or full operational support** in all environments.

### Current Status

The technology is under active refinement.
Interfaces, behaviors, and supported features may change as the evaluation and research process continues.

In particular:

* APIs may change without prior notice
* implementation details may evolve
* performance depends on dataset characteristics, hardware, and configuration
* some advanced operational features may still be under investigation or refinement

### Support and Customization

This repository is provided for evaluation purposes.

Individual technical support, integration assistance, customization, tuning, and other project-specific services are **not included by default** and are available **only on a paid basis under a separate agreement**.

Typical examples include:

* support for evaluation and benchmarking
* adaptation to specific datasets or workflows
* performance tuning
* integration assistance
* customization for domain-specific requirements

If you are interested, please contact us at:  
[tokada@fuutasystemservice.com](mailto:tokada@fuutasystemservice.com)

### Important Notes

This library is designed to help users evaluate the underlying technology responsibly and transparently.
This library is published not as a claim of universal superiority over existing systems, but as an opportunity for independent verification and technical assessment.

We welcome constructive feedback from users who evaluate the library in real conditions.

## Licensing

The licensing of this repository is intentionally split by component.

### Core DLL

The core Re-kNN binary library (DLL) is distributed under a **proprietary evaluation license**.

This means:

- it may be used for evaluation, benchmarking, feasibility studies, research, validation, and prototyping
- it is **not** provided as open source
- it is **not** intended for production use under the evaluation license
- support, customization, integration assistance, and commercial use require a separate agreement

Please see:  
[`EULA-RekNN-Evaluation.md`](./EULA-RekNN-Evaluation.md)

### Sample Applications

CLI-based sample applications and related sample source code are provided under the **Apache License 2.0**.

Please see:  
[`LICENSE`](./LICENSE)

### Documentation

Unless otherwise noted, the documentation in this repository is provided under the same license as the sample applications.

### Notes

This repository is published to support responsible technical evaluation and independent verification.
The core library is intentionally distributed as a binary-only evaluation package, while sample code is provided separately to improve transparency and usability.


## Current Evaluation Status

The following evaluations are currently available:

* BERT (Large-Scale Text Vector Database / Wikipedia JP)
* MNIST
* CIFAR-10

For details, please refer to the following documents:

- [Evidence Overview](./EVIDENCE.md)
- [Evidence for MNIST](./EVIDENCE.MNIST.md)
- [Evidence for CIFAR-10](./EVIDENCE.CIFAR-10.md)
- [Evidence for BERT](./EVIDENCE.BERT.md)
- [Evidence for MNIST Clustering](./EVIDENCE.MNIST.Clustering.md)

Evaluation results will be updated incrementally over time.

## Code Snippet

Re-kNN provides a compact API for registration, search, deletion, and refinement.

```csharp
// 1. Initialize the engine
Initialize(ModeEnum.MNIST, 1);

// 2. Register a vector
Add(0, vec, length, mainId, subId, 5, 0.9);

// 3. Run inference
PredictResult* result = Predict(0, vec, length, kValue, 0.7);

// 4. Delete a document
Delete(0, mainId, subId);

// 5. Rebuild / refine if needed
Refine(0, vec, length, mainId, subId, 5, 0.9);
```

For API details, please refer to the following file:

[API](./API.md)
